"""scout_daemon — The Tribe's income-discovery sensor.

A PURE SENSOR. It discovers, classifies, and ranks methods of income generation
available to an autonomous agent tribe, and surfaces them for admission.

It does not execute work. It does not create accounts, submit applications,
contact counterparties, or use credentials. It does not test, probe, or scan
any target system -- including targets inside a published white-hat scope,
where it captures the scope text and stops. Execution belongs to a later
Builder daemon; identity and money belong to the Treasurer.

Read-only against every external surface, with no write-capable tool bound to
any source. That is the containment boundary, not a performance guideline.
"""

__version__ = "0.1.0"
